-- local isabled = function()
--   if vim.version().major == 0 and vim.version().minor < 10 then
-- 	return false
--   end
-- end

return {
	"Bekaboo/dropbar.nvim",
	version = "*",
	event = "LazyFile",
	-- enabled = isabled(),
}
